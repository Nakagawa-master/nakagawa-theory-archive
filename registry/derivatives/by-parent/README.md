# By Parent Ledgers

This directory stores derivative ledgers grouped by parent NCL ID.

Example structure:

```text
registry/derivatives/by-parent/NCL-YYYYMMDD-0001/ledger.md
```

Each ledger must record the parent NCL ID, parent Diff ID, WordPress post ID, repository path, publication status, and published URL when available.
